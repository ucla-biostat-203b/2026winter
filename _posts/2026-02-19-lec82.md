---
layout: post_teaching
title: Lecture 9
category: biostat203bwinter2026lec82
---

## Today

* Shiny.

* tidymodels.

## Announcement

* [HW3](https://ucla-biostat-203b.github.io/2026winter/hw/hw3/hw3.html) due Feb 22 @ 11:59pm. Start early!

* [HW4](https://ucla-biostat-203b.github.io/2026winter/hw/hw4/hw4.html) due Mar 11 @ 11:59pm. Start early!

* Posit Assistant in RStudio! [Announcement](https://posit-ai-beta.share.connect.posit.cloud/).

## Q&A

* Feedback on HW2 (Thanks to TA Yucai Shao). Median score 190.5, IQR [180, 197] out of 200.

    * Q1.2: Many students used `col_character()` for categorical columns instead of `col_factor()` so their tibble exceeded the 50MB memory target.
    
    * Q2.4–Q2.6: The most common deduction was omitting `arrange(subject_id, charttime, itemid)`, causing inconsistent output ordering across questions (Tomoki flagged this on Slack). Deducted 5 points once; no penalty for not adjusting time zones.
    
    * Q2.4–Q2.6: Another recurring issue was students filtering/selecting on the already-filtered labevents data rather than working with the full `labevents.csv` or `labevents.parquet` as intended. This bypasses the point of demonstrating Arrow/Parquet/DuckDB on the original large dataset.

    * Q2.3/Q3: Incorrect `awk` column indices for filtering.
    
    * Style: Missing spaces around operators (`%>%`, `<-`) or after commas in `c()` calls were the most frequent style deductions.

* Q: There is a discrepancy: HW2 Q3 asks for `mean non-invasive blood pressure (220181)`, while HW3 Q1.2 displays `Non Invasive Blood Pressure diastolic (220180)`.

    A: For HW3, please use `220180`.
    
* Q: In HW3 Q6, find the first vital measurements for each ICU stay according to `storetime`. However, the same vital measured at multiple times can be deposited to the patient's chart at the same `storetime`. How should we handle this?

    A: Use the average vital measurement corresponding to the first `storetime` of each ICU stay.